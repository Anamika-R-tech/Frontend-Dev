<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pseudo-Classes Demonstration</title>
  <style>
    /* ---------- Base Styles ---------- */
    body {
      font-family: Arial, sans-serif;
      background-color: #f8f9fa;
      margin: 0;
      padding: 40px;
      color: #333;
    }

    h1 {
      text-align: center;
      margin-bottom: 40px;
      color: #0d6efd;
    }

    /* ---------- Flexbox Layout ---------- */
    .examples {
      display: flex;
      justify-content: space-around;
      align-items: flex-start;
      flex-wrap: wrap;
      gap: 30px;
    }

    .example {
      background: #fff;
      border-radius: 10px;
      padding: 20px;
      width: 250px;
      text-align: center;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }

    .example:hover {
      transform: translateY(-5px);
    }

    /* ---------- :hover Example ---------- */
    a {
      text-decoration: none;
      color: #0d6efd;
      font-weight: bold;
      font-size: 1.1rem;
    }

    a:hover {
      color: #dc3545;
    }

    /* ---------- :focus Example ---------- */
    input {
      padding: 10px;
      border: 2px solid #ccc;
      border-radius: 5px;
      outline: none;
      transition: 0.3s;
    }

    input:focus {
      border-color: #0d6efd;
      box-shadow: 0 0 5px rgba(13, 110, 253, 0.5);
    }

    /* ---------- :active Example ---------- */
    button {
      background-color: #198754;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      font-size: 1rem;
      cursor: pointer;
      transition: 0.3s;
    }

    button:active {
      background-color: #145c32;
      transform: scale(0.95);
    }

    /* ---------- :nth-child() Example ---------- */
    ul {
      list-style-type: none;
      padding: 0;
      margin: 0;
    }

    ul li {
      background: #e9ecef;
      margin: 5px 0;
      padding: 10px;
      border-radius: 5px;
    }

    ul li:nth-child(odd) {
      background-color: #cfe2ff;
    }

    ul li:nth-child(even) {
      background-color: #e2e3e5;
    }

    /* ---------- Description Text ---------- */
    .desc {
      margin-top: 15px;
      color: #555;
      font-size: 0.9rem;
    }
  </style>
</head>

<body>
  <h1>CSS Pseudo-Classes Demonstration</h1>

  <div class="examples">
    <!-- Hover Example -->
    <div class="example">
      <h3>:hover</h3>
      <a href="#">Hover Over Me</a>
      <p class="desc">The <b>:hover</b> pseudo-class changes style when the mouse hovers over an element.</p>
    </div>

    <!-- Focus Example -->
    <div class="example">
      <h3>:focus</h3>
      <input type="text" placeholder="Click to focus" />
      <p class="desc">The <b>:focus</b> pseudo-class applies when an element (like an input) is selected or focused.</p>
    </div>

    <!-- Active Example -->
    <div class="example">
      <h3>:active</h3>
      <button>Click Me</button>
      <p class="desc">The <b>:active</b> pseudo-class styles an element while it's being clicked or activated.</p>
    </div>

    <!-- nth-child Example -->
    <div class="example">
      <h3>:nth-child()</h3>
      <ul>
        <li>Item 1</li>
        <li>Item 2</li>
        <li>Item 3</li>
        <li>Item 4</li>
      </ul>
      <p class="desc">The <b>:nth-child()</b> pseudo-class targets elements based on their position in a list.</p>
    </div>
  </div>
</body>
</html>

